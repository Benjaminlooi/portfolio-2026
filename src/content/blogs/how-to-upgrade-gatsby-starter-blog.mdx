---
title: "How to upgrade Gatsby starter blog (or any other gatsby starter template)"
summary: "This article describes how to upgrade Gatsby starter blog (or any other gatsby starter template)"
metaDescription: "Upgrade your Gatsby starter blog template by merging upstream changes using git remote, fetch, and merge with the --allow-unrelated-histories flag."
image: "https://images.unsplash.com/photo-1618401471353-b98afee0b2eb?w=1200&h=630&fit=crop"
author: "Benjamin Looi"
publishedAt: "2021-04-26"
tags: ["gatsby", "git", "blogging", "web-development", "static-site"]
---

Use good git hygiene and create a new branch to merge the upstream so you can test for breaking changes:
`git checkout -b upstarter`

Add the remote to upstream:
`git remote add upstream git@github.com:gatsbyjs/gatsby-starter-blog.git`

Fetch the upstream:
`git fetch upstream`

Then when you merge it use the --allow-unrelated-histories argument:
`git merge upstream/master --allow-unrelated-histories`

You'll most likely have conflicts to resolve, but after that you can merge it back into master:

```bash
git checkout master
git merge upstarter
```